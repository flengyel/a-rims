/*global window,L,CUR*/
/*jslint plusplus: false */
CUR = window.CUR || {};

CUR.MapLayer = Backbone.Model.extend({
    // Default attributes for the todo item.
     //ATTRIBUTES
     //user_name: 'cunycur',
     //table_name: 'TABLENAME',
     //query: "SELECT * FROM {{table_name}}",
     //opacity: 1,
     //zIndex: 11   
    
    defaults: function() {
        return {
           
        };
    },
    
    initialize: function(options) {
       
               
    }


});